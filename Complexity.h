#pragma once
#include <iostream>

// 복잡도 사용하는 이유 : 둘 이상의 자료구조를 수치적으로 비교하기 위한 도구

// 시간 복잡도 : 실행 시간. 코드에 연산 횟수 측정

// 공간 복잡도 : 메모리 공간을 두 분류로 나눈다. 문제 해결에 필요 없는 메모리, 필요한 메모리.


void Print();

void lecture1();

void Example01();